# Unity-AR-3D-Model-Generation-
This project demonstrates an AR pipeline in Unity that captures 2D images, processes them in the cloud to generate 3D models, and loads them into the scene dynamically. Key steps include image capture, API routing via Railway, 3D model generation on Google Colab, and runtime loading of the generated .obj mesh into the Unity environment.
